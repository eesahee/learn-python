# if using python v2 must use raw_input instead of input to avoid errors

person = raw_input('Enter your name: ')

print 'Hello', person

number = int(raw_input("Number:"))

print "The value of my time is" , number , " dollars per hour."
